{
  "name": "server",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "find . -regex '^.*\\(__pycache__\\|\\.py[co]\\)$' -delete",
    "clean:all": "pnpm run clean && rm -rf .venv/",
    "dev": ".venv/bin/python3 src/main.py",
    "prepare": "python3 -m virtualenv .venv/ && .venv/bin/python3 -m pip install -r requirements.txt && (yes \"Restart VSCode if it can't find the Python Modules!\" | head -n 5)",
    "start": ".venv/bin/python3 src/main.py -prod"
  }
}
